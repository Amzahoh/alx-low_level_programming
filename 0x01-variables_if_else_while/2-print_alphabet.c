#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
        char alphi = 'a';

        while (alpha <= 'z')
        {
                putchar(alph);
                alph++;
        }
                putchar('\n');
        return (0);
}
